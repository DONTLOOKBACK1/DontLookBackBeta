.github/workflows/build_game.yml
jobs:
  build:
    name: Export Godot Project
    runs-on: windows-latest   # Runner Windows

    steps:
      # Paso 1: Clonar el repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      # Paso 2: Exportar con Godot
      - name: Export with Godot Export Action
        uses: firebelley/godot-export@v7.0.0
        with:
          godot-version: '4.5.1-stable'
          export-presets: './export_presets.cfg'   # Ajusta según la ubicación real
          preset: 'Windows Desktop'
          output-dir: 'build'
          export-path: 'build/DontLookBackBeta.exe'
          template-download: true
          debug: false

      # Paso 3: Verificar build generado
      - name: List build folder
        run: dir build   # En Windows se usa 'dir' en lugar de 'ls'

      # Paso 4: Crear carpeta artifacts (si no existe)
      - name: Create artifacts folder
        run: powershell New-Item -ItemType Directory -Force -Path artifacts

      # Paso 5: Crear zip del build
      - name: Create zip of build
        run: powershell Compress-Archive -Path build\* -DestinationPath artifacts\windows-build.zip -Force

      # Paso 6: Verificar que el zip se haya creado
      - name: Verify artifact file exists
        run: dir artifacts

      # Paso 7: Subir artifact
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: artifacts/windows-build.zip

